---
name: doc-check
description: 커뮤니티·보조금 사업 단체별 제출 서류를 폴더에서 스캔해 누락 서류를 한 표로 보여주고 현황판을 발행한다. "서류 점검", "서류 확인해줘", "누락 서류", "제출 현황", "정산 서류 빠진 것", "미제출 단체", "서류 현황판" 같은 요청에 사용한다.
---

# 서류 누락 점검

단체별 폴더의 **파일명**을 체크리스트와 대조해 빠진 서류를 찾고, 한눈에 보이는 현황판을 만든다.

## 준비물 확인

기준 체크리스트: `references/checklist.md` (단계 / 서류명 / 필수·선택 / 파일명 키워드)
점검 대상 폴더 기본값: `config.json` 의 `defaultRoot`

`config.json` 이 없고 사용자가 경로를 말하지 않았다면, AskUserQuestion 대신 짧게 경로를 물어본다(자유 입력값이라 선택지로 만들 수 없다). 경로를 받으면 다음 형태로 저장해 다음부터 묻지 않는다:

```json
{ "defaultRoot": "D:/2026 커뮤니티사업/단체별서류" }
```

## 폴더 구조 가정

기본은 **점검 폴더 아래 하위 폴더 하나가 단체 하나**다. 단체 폴더 안의 하위 폴더는 깊이 제한 없이 모두 뒤진다.

```
2026 커뮤니티사업/
├─ 가온마을 공동체/        ← 단체
│  ├─ 참여신청서.pdf
│  └─ 정산/영수증.pdf      ← 하위 폴더도 함께 스캔
└─ 충주 청년기획단/
```

한 단체만 점검할 때는 그 단체 폴더를 `--root` 로 주고 `--flat` 을 붙인다.

## 실행 순서

### 1. 스캔

```bash
node "$HOME/.claude/skills/doc-check/scripts/scan-docs.mjs" \
  --root "<점검 폴더>" \
  --json "<스크래치패드>/doc-report.json"
```

Windows에서는 PowerShell 도구로 절대 경로를 그대로 쓴다:

```powershell
node "C:\Users\love-\.claude\skills\doc-check\scripts\scan-docs.mjs" --root "<점검 폴더>" --json "<스크래치패드>\doc-report.json"
```

옵션:
- `--stage "정산"` — 특정 단계만 점검 (부분 일치)
- `--flat` — 점검 폴더 자체를 단체 하나로 취급
- `--checklist <경로>` — 다른 체크리스트 사용 (사업별로 표를 따로 둘 때)

### 2. 현황판 만들기

```bash
node "$HOME/.claude/skills/doc-check/scripts/build-dashboard.mjs" \
  --report "<스크래치패드>/doc-report.json" \
  --out "<스크래치패드>/서류현황판.html"
```

### 3. 발행

Artifact 도구로 생성된 HTML을 발행한다. 템플릿에 디자인이 이미 들어 있으므로 `artifact-design` 스킬을 다시 로드하지 않는다.

- 같은 사업을 다시 점검할 때는 **이전과 같은 파일 경로**로 만들어 같은 URL에 덮어쓴다. 이전 대화의 현황판을 갱신하라는 요청이면 `action: "list"` 로 URL을 찾아 `url` 로 넘긴다.
- `favicon` 은 첫 발행에만 넘긴다.

### 4. 보고

터미널에는 표가 아니라 행동할 것만 짧게 적는다:
- 필수 서류가 완비된 단체 수 / 보완 필요한 단체 수
- 빠진 단체가 가장 많은 서류 상위 3개 (단체 공지 한 번으로 해결되는 항목)
- 현황판 링크

## 결과가 이상할 때

이 스킬은 파일 내용을 열지 않고 **파일명·폴더명만** 본다. 그래서 두 가지가 생긴다.

| 증상 | 대응 |
|---|---|
| 제출했는데 누락으로 나온다 | 그 서류의 키워드에 실제 파일명에 쓰인 표현을 추가한다 |
| 엉뚱한 파일이 제출로 잡힌다 | 키워드를 더 구체적으로 좁힌다 (예: `신청서` → `참여신청`) |

현황판에서 칸을 누르면 매칭된 파일명이 보이므로 오탐을 바로 확인할 수 있다. 사용자가 "이건 아닌데"라고 하면 `references/checklist.md` 의 해당 행만 고치고 다시 스캔한다.

## 체크리스트 수정 요청 처리

"○○ 서류도 받아야 해" / "이건 이제 안 받아" 같은 요청은 `references/checklist.md` 의 표를 직접 수정한다. 새 행을 추가할 때는 키워드를 실제로 단체가 쓸 파일명 표현으로 2~4개 넣는다. 수정 후에는 다시 스캔해 결과가 어떻게 달라졌는지 보고한다.
